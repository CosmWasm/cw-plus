{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "InitMsg",
  "type": "object",
  "required": [
    "decimals",
    "name",
    "reserve_denom",
    "symbol"
  ],
  "properties": {
    "decimals": {
      "description": "decimal places of the supply token (for UI)",
      "type": "integer",
      "format": "uint8",
      "minimum": 0.0
    },
    "name": {
      "description": "name of the supply token",
      "type": "string"
    },
    "reserve_denom": {
      "description": "this is the reserve token denom (only support native for now)",
      "type": "string"
    },
    "symbol": {
      "description": "symbol / ticker of the supply token",
      "type": "string"
    }
  }
}
